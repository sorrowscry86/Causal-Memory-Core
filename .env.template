# Causal Memory Core Configuration Template
# Copy this file to .env and fill in your actual values

# OpenAI API Configuration (Required)
OPENAI_API_KEY=your_openai_api_key_here

# Database Configuration
DB_PATH=causal_memory.db

# Embedding Model Configuration
EMBEDDING_MODEL=all-MiniLM-L6-v2
EMBEDDING_DIMENSION=384

# LLM Configuration
LLM_MODEL=gpt-3.5-turbo
LLM_TEMPERATURE=0.1

# Search and Similarity Configuration
MAX_POTENTIAL_CAUSES=5
# SIMILARITY_THRESHOLD: Controls how similar events must be to be considered related (0.0-1.0)
# Default is 0.5 (balanced). Increase to 0.7+ for stricter matching and fewer false positives.
# Decrease to 0.3-0.4 for broader matching if events are semantically diverse.
SIMILARITY_THRESHOLD=0.5
TIME_DECAY_HOURS=24

# HTTP API Server Configuration
PORT=8000
HOST=0.0.0.0

# API Security (Optional)
# Uncomment to require API key authentication
# API_KEY=your-secret-api-key-here

# CORS Configuration (comma-separated list of allowed origins)
# Use "*" to allow all origins (default)
CORS_ORIGINS=*

# MCP Server Configuration
MCP_SERVER_NAME=causal-memory-core
MCP_SERVER_VERSION=1.1.1
